<div class="panel" >
  <h3>Editing: <%= @topic_group.name %></h3>
  <div>
	<img src= <%= @topic_group.bar_chart %> />
	
	<%= render 'form' %>

	<%= link_to 'Back', edit_topic_path(@topic_group.topic_id) %>
  </div>
</div>
