<%= form_for(@grouping) do |f| %>
  <% if @grouping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grouping.errors.count, "error") %> prohibited this grouping from being saved:</h2>

      <ul>
      <% @grouping.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br />
    <%= f.text_field :company_id %>
  </div>
  <div class="field">
    <%= f.label :parent_id %><br />
	<%= f.select :parent_id, nested_set_options(Grouping, @grouping) {|i| "#{'-' * i.level} #{i.name}" }, {:include_blank => 'None'} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
